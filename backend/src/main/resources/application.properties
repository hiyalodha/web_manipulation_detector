# Web Manipulation Detector — Backend Config

# Server port
server.port=8080

# App name (shows in logs)
spring.application.name=manipulation-detector

# Pretty-print JSON responses (helpful for debugging)
spring.jackson.serialization.indent-output=true

# Log level — set to DEBUG for more verbose output during development
logging.level.root=INFO
logging.level.com.detector=DEBUG
